#include<stdio.h>
int main(void)
{
	int n1, n2, i, j;

	printf("输入两个正整数\n");
	scanf("%d %d", &n1, &n2);
	for (i = 1; i <= n1 && i <= n2; i++)
	{
		if (n1 % i == 0 && n2 % i == 0)
			j = i;
	}
	printf("%d和%d的最大公约数是%d", n1, n2, j);
	return 0;
}
