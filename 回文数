#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
int main(void)
{
	int i, j, k = 0, l;

	printf("请输入一个整数:");
	scanf("%d", &i);
	j = i;
	while (i != 0)
	{
		l = i % 10;
		k = k * 10 + l;
		i /= 10;
	}
	if (k == j)
		printf("%d是回文数", j);
	else
		printf("%d不是回文数", j);
}
